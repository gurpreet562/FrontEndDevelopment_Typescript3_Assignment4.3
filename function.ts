 
 import { EmployeeDetailsSpace } from './interface'
 
 export function EmployeeDetail(detail:EmployeeDetailsSpace.Employee) {
    console.log(` The Employee details are : Name ${detail.name} , 
     the City is ${detail.city} and the State is ${detail.state}`);
  }
    export function employee(){
      console.log("The employee the five rewards eithin three months");
    
    }



  

